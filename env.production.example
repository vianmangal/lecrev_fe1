# Leave blank when the frontend is served by nginx on the same origin as the backend.
VITE_LECREV_API_BASE_URL=""

# Replace with a tenant-scoped or demo API key before building for deployment.
VITE_LECREV_API_KEY="replace-me"

# Default project loaded into the dashboard settings form.
VITE_LECREV_PROJECT_ID="demo"
